# Node dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json (standard is to commit this, but we can leave it unignored)

# Electron-builder / packaging output
dist/
out/
app-update.yml

# SQLite database files (just in case they are generated or backed up in the workspace)
*.db
*.db-journal
*.db-wal
*.db-shm
*.sqlite

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDEs and editors
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp

/plan.md
/structure.md
/Projectt Context.md

# --- Dependencies & build output ---
node_modules/
dist/
build/
out/
release/

# --- SENSITIVE: SQLite database files (contains real buyer/seller/invoice data) ---
*.sqlite
*.sqlite3
*.db
*.db-journal
*.db-wal
*.db-shm
database/*.sqlite
database/*.db
database/data/

# --- SENSITIVE: any local backups of the DB if they ever land inside the repo ---
*backup*.sqlite
*backup*.db
backups/

# --- SENSITIVE: generated invoice PDFs if exported into project folder ---
*.pdf
exports/
generated-invoices/

# --- Env / secrets (in case you add API keys, license keys, etc later) ---
.env
.env.local
.env.*.local

# --- OS/editor noise ---
.DS_Store
Thumbs.db
.vscode/
.idea/

# --- Electron packaging artifacts ---
*.exe
*.dmg
*.AppImage
*.deb
*.snap

# --- Logs ---
*.log
npm-debug.log*